#ifndef SPOTIFY_UTILS_H
#define SPOTIFY_UTILS_H

#include "includes.h"
#include "spotify_command_defines.h"
#include "spotify_structs.h"

char* url_encoder(char *, const int search_type);
char* concat_strings(char *, char *);

#endif
